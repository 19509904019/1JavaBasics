1. File类的理解
    > File类位于java.io包下，本章中涉及到的相关流也都声明在java.io包下
    > File类的一个对象，对应与操作系统下的一个文件或一个文件目录(或文件夹)
    > File类中声明了新建、删除、获取名称、重命名等方法， 并没有涉及文件内容的读写操作。要想实现文件内容
      的读写，就需要使用IO流
    > File类的对象，通常是作为io流操作的文件的端点出现的
        > 代码层面，将File类的对象作为参数传递到IO流相关类的构造器中

2. 内部api使用说明
    2.1 构造器


    2.2 方法

