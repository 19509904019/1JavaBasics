[面试题] ArrayList、Vector、LinkedList的三者的对比？
层次一：
    ArrayList:List的主要实现类；线程不安全的、效率高；底层使用Object[]数组存储
              在添加数据、查找数据时，效率较高；在插入、删除数据时，效率较低

    Vector:List的古老实现类，线程安全的、效率低；底层使用Object[]数组存储

    LinkList:底层使用双向链表的方式进行存储；在添加数据、查找数据时，效率较低；在插入、删除数据时，效率较高
             在对集合中的数据进行频繁的删除、插入操作时，建议使用此类；

层次二：
    查看相关api的源码

[面试题] HashMap的底层源码的剖析；LinkedHashMap的底层源码的剖析

HashMap的长度为什么是2的幂次方？
    当哈希表的长度为 2 的幂次方时，通过使用位运算来计算索引位置，
    可以将取模操作（%）转换为按位与操作（&），从而提高计算索引效率。

HashMap什么时候扩充为红黑树，什么时候又返回到链表？
    索引i的位置的链表长度超过8且数组长度达到64时，索引i位置需要转为红黑树
    当索引i的位置元素的个数低于6时，要红黑树结构转为单向链表，从而节省空间，红黑树的存储空间是链表的两倍
