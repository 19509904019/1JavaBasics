1.System类
     >属性：out、in、err
     >方法：currentTimeMillis() / gc() / exit(int status) / getProperty(String property)

 2.Runtime类
    对应着Java进程的内存使用的运行时环境，是单例的

 3.Math类
    凡是与数学运算相关的操作，都可以在此类中找到相关的方法

 4.BigInteger类和BigDecimal类
    BigInteger:可以表示任意长度的整数
    BigDecimal:可以表示任意精度的浮点数

 5.Random类
